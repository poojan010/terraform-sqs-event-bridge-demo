{
  "name": "aws-event-driven-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist *.zip",
    "zip": "zip -j terraform/producer.zip dist/producer/index.js && zip -j terraform/consumer.zip dist/consumer/index.js",
    "deploy": "npm run clean && npm run build && npm run zip"
  },
  "dependencies": {
    "@aws-sdk/client-eventbridge": "^3.600.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.137",
    "@types/node": "^24.7.0",
    "typescript": "^5.6.3"
  }
}
